# No API keys or third-party login credentials are required.
# Local database is persisted in .wrangler/state (ignored by Git).
# Production DB is injected as a Cloudflare D1 binding, not an environment URL.
# Optional local test runner target:
TEST_URL=http://127.0.0.1:5173
