import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const users = sqliteTable('users', { id: text('id').primaryKey(), email: text('email').notNull().unique(), name: text('name').notNull(), password: text('password').notNull(), salt: text('salt').notNull(), timezone: text('timezone').notNull(), theme: text('theme').notNull().default('daylight'), badge: text('badge'), createdAt: text('created_at').notNull() });
export const sessions = sqliteTable('sessions', { token: text('token').primaryKey(), userId: text('user_id').notNull().references(() => users.id), expiresAt: integer('expires_at').notNull() }, t => [index('idx_sessions_user').on(t.userId)]);
export const tasks = sqliteTable('tasks', { id: text('id').primaryKey(), userId: text('user_id').notNull().references(() => users.id), title: text('title').notNull(), category: text('category').notNull(), difficulty: text('difficulty').notNull(), dueDate: text('due_date'), createdAt: text('created_at').notNull(), deleted: integer('deleted').notNull().default(0) }, t => [index('idx_tasks_user').on(t.userId)]);
export const completions = sqliteTable('completions', { id: text('id').primaryKey(), taskId: text('task_id').notNull().unique().references(() => tasks.id), userId: text('user_id').notNull().references(() => users.id), title: text('title').notNull(), category: text('category').notNull(), xp: integer('xp').notNull(), gold: integer('gold').notNull(), day: text('day').notNull(), createdAt: text('created_at').notNull() }, t => [index('idx_completions_user_day').on(t.userId, t.day)]);
export const purchases = sqliteTable('purchases', { id: text('id').primaryKey(), userId: text('user_id').notNull().references(() => users.id), itemId: text('item_id').notNull(), price: integer('price').notNull(), createdAt: text('created_at').notNull() }, t => [uniqueIndex('idx_purchases_user_item').on(t.userId, t.itemId)]);
export const limits = sqliteTable('rate_limits', { key: text('key').primaryKey(), attempts: integer('attempts').notNull(), expires: integer('expires').notNull() });
